# Define lists variable
lists = [
    [3, 4, 5, 6],  # List with positive integers
    [-1, -2, -3, -4],  # List with negative integers
    [0, 0, 0, 0],  # List with all elements as 0
    [],  # Empty list
]

# Find the maximum and minimum values for each test case
if not lists[0]:
    print("None")
else:
    max_value = max(lists[0])
    min_value = min(lists[0])
    print(max_value, min_value)

if not lists[1]:
    print("None")
else:
    max_value = max(lists[1])
    min_value = min(lists[1])
    print(max_value, min_value)

if not lists[2]:
    print("None")
else:
    max_value = max(lists[2])
    min_value = min(lists[2])
    print(max_value, min_value)

if not lists[3]:
    print("None")
else:
    max_value = max(lists[3])
    min_value = min(lists[3])
    print(max_value, min_value)
